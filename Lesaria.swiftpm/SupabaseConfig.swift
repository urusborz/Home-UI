import Foundation

enum SupabaseConfig {
    // These defaults are intentionally empty. Enter the values in the app settings,
    // or replace them locally if you are building a private-only copy.
    static let defaultProjectURL = ""
    static let defaultAnonKey = ""
    static let defaultSyncID = "lesaria-main"
    static let defaultSyncToken = ""
}
